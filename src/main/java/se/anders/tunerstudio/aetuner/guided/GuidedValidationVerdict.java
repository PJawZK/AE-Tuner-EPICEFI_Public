package se.anders.tunerstudio.aetuner.guided;

/** Conservative method-owned verdict for one post-Apply A/B validation. */
public enum GuidedValidationVerdict {
    KEEP,
    RESTORE,
    INCONCLUSIVE
}
